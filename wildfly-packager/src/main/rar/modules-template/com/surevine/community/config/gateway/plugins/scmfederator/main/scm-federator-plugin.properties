scm.federator.api.base.url=http://localhost:8080/scm-federation/api

# Perform sanitisation checks on outbound archives (SCM only)
sanitisation.enabled=false
sanitisation.service.base.url=http://localhost:9000/api